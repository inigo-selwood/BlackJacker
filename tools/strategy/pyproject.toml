[project]
name = "blackjacker-strategy"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["black>=25.0", "PyYAML>=6.0", "pytest>=8.0", "SQLAlchemy>=2.0"]

[tool.black]
line-length = 79
target-version = ["py313"]
